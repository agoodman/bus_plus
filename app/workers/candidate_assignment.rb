class CandidateAssignment

  def self.send_assignment(passenger_id,vehicle_id)
    puts "TODO: send APNs assignment to passenger #{passenger_id} and vehicle #{vehicle_id}"
    # TODO grocer integration
  end
  
end
